name: mailingroom
description: A new Flutter project for managing mailroom operations.
publish_to: 'none' # Mencegah publikasi ke pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Memastikan kompatibilitas Dart SDK

dependencies:
  flutter:
    sdk: flutter
  
  # Manajemen state
  provider: ^6.1.1
  
  # UI dan utilitas
  font_awesome_flutter: ^10.7.0
  flutter_staggered_animations: ^1.1.1

  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

flutter_staggered_animations: ^1.1.1
  